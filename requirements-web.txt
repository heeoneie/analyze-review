# 답글 생성 화면만 배포할 때 필요한 최소 의존성.
# 분석/크롤링 라우터는 backend/main.py 에서 import 실패 시 자동으로 빠진다.
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
openai>=1.0.0
python-dotenv>=1.0.0
# 계정·답글 이력 저장. SQLite 는 파이썬 내장이라 드라이버가 따로 필요 없다.
sqlalchemy>=2.0.0,<3.0.0
# 기동 때 스키마를 head 까지 올린다. backend/main.py 가 직접 import 한다.
alembic>=1.13.0,<2.0.0
# 세션 쿠키 서명
itsdangerous>=2.0.0
# 카카오 토큰 교환. openai 가 httpx2 를 쓰므로 httpx 는 따로 있어야 한다.
httpx>=0.27.0
# 리뷰 수집. 쿠팡은 Akamai 가 TLS 지문으로 막아서 Chrome 위장이 필요하다.
# 대시보드가 사장님이 쓰는 화면이 된 이상 슬림 배포에도 있어야 한다.
curl_cffi>=0.7.0,<1.0.0
